#!/usr/bin/env sh

echo "🔍 Running pre-commit checks..."

# Run format, lint, and tests
make check

# Check if make check was successful
if [ $? -ne 0 ]; then
  echo "❌ Pre-commit checks failed. Please fix the issues and try again."
  exit 1
fi

echo "✅ All pre-commit checks passed!"
